================================================================================
                        تعليمات مشروع "مقاضي"
================================================================================

⚠️ تنبيه مهم: في كل مرة تُفتح فيها جلسة جديدة، شغّل السكريبت التالي:

cd /home/z/my-project && bash /home/z/my-project/download/APPLY_FIXES.sh

================================================================================
                        الملفات المهمة للمشروع
================================================================================

1. الملفات الأساسية:
   - package.json
   - next.config.ts  
   - tsconfig.json
   - prisma/schema.prisma

2. الملفات المعدلة:
   - src/app/page.tsx (حقل الكمية فارغ + زر حذف الاقتراحات)
   - src/app/api/suggestions/delete/route.ts (API حذف الاقتراحات)

================================================================================
                        التحديثات المهمة
================================================================================

✅ حقل الكمية فارغ افتراضياً (بدون رقم 1)
✅ placeholder = "أدخل الكمية"
✅ زر حذف الاقتراحات 🗑️
✅ حذف الاقتراحات من السيرفر والأجهزة الأخرى
✅ نموذج SavedProductName في قاعدة البيانات

================================================================================
                        أوامر مهمة
================================================================================

تشغيل المشروع:
  npm run dev

تحديث قاعدة البيانات:
  npx prisma generate
  npx prisma db push

حفظ التعديلات في git:
  git add .
  git commit -m "رسالة التعديل"

================================================================================
